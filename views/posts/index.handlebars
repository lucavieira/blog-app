<div class="card">
    <div class="card-body">
        <h2>{{post.title}}</h2>
        <hr>
        <small>Publicado em - {{post.create_date}}</small>
        <hr>
        <p>{{post.content}}</p>
    </div>
</div> 